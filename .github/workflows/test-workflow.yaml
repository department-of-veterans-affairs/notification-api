name: Test workflow

on:
  workflow_run:
    workflows: 
      - Development CI
      - dev-ci.yaml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print event
        run: |
          echo ${{ toJson(github.event) }}